import { createStitches } from "@stitches/react";

export const { getCssText } = createStitches({
  theme: {
    colors: {
      primary: "#FAFAFA",
      secondary: "#EFEFEF",
      contrast: "#191919",
      punch: "#EE4E4E",
    },
  },
});
